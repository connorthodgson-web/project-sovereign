# Email Notes

Use this folder for outbound and inbound email setup.

Good things to record here:
- SMTP credentials
- sender identity
- app passwords
- transactional email provider values
- inbox access values for reading messages later

If you are unsure whether a provider will use SMTP or API later, fill both sections now if possible.

# Calendar Notes

Use this folder for calendar-related credentials and defaults.

Recommended contents:
- Google OAuth app values
- Microsoft Graph app values
- primary calendar IDs
- default timezone and reminder behavior

If one provider ends up being the main source of truth, keep the secondary provider filled anyway so future switching is easier.
